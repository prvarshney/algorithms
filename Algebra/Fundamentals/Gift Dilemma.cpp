#include <iostream>

using namespace std;
int 
main(int argc, char* argv[]) {
    int t, a, b, c, p;
    cin >> t;
    for(int iter = 1 ; iter <= t ; iter++) {
        cin >> a >> b >> c >> p;
        int res;
        printf("Case %d: %d\n", iter, res);
    }
    return 0;
}